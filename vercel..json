{
    "buildCommand": "cd client && npm install && npm run builder",
    "outputDirectort": "client/dist",
    "framework": "react",
    "rewrites": [
        {
            "source": "/api(.*)",
            "destination": "/api/index.js"
        }
    ]
}